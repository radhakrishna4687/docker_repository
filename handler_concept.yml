---
 - hosts: nodes
   gather_facts: false
   tasks: 
      - name: Installing apache2
        apt:
          name: apache2 
          state: present 
        notify: 
          - start apache2
   handlers:
      - name: start apache2 
        service: 
          name: apache2 
          state: started 


               
        #register: out 
      #- debug: var = out.changed  
      #- name: start apache2 
       # service:  
        #  name: apache2 
         # state: started
        #when: out.changed == True 
